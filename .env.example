# Database connection string matching docker-compose setup
DATABASE_URL="postgresql://postgres:postgrespassword@localhost:5432/luxe_db?schema=public"

# Secret key used for signing and verifying JSON Web Tokens (JWT)
JWT_SECRET="luxe_super_secret_jwt_key_2026_9f8e7d6c5b4a3f2e1d0c9b8a7f6e5d4c"

# Server Port
PORT=5000

# Google OAuth credentials 
GOOGLE_CLIENT_ID="your_google_client_id_here"
GOOGLE_CLIENT_SECRET="your_google_client_secret_here"
GOOGLE_CALLBACK_URL="http://localhost:5000/auth/google/callback"
FRONTEND_URL="http://localhost:3000"
# Admin Initial Credentials (Seeding)
ADMIN_EMAIL="admin@yopmail.com"
ADMIN_PASSWORD="AdminPass123!"

# ==============================================================================
# Nodemailer SMTP Configuration (Industry Standard)
# ==============================================================================
# Option 1: Gmail SMTP Setup
#   SMTP_HOST="smtp.gmail.com"
#   SMTP_PORT=587 (or 465 for SSL)
#   SMTP_USER="your-email@gmail.com"
#   SMTP_PASS="16-character-google-app-password" (Generate at https://myaccount.google.com/apppasswords)
#   SMTP_SECURE="false" (set to "true" if using port 465)
#   SMTP_FROM='"Luxe E-Commerce" <your-email@gmail.com>'
#
# Option 2: Mailtrap / Ethereal (For Testing)
#   SMTP_HOST="sandbox.smtp.mailtrap.io"
#   SMTP_PORT=2525
#   SMTP_USER="your_mailtrap_user"
#   SMTP_PASS="your_mailtrap_pass"
#   SMTP_SECURE="false"
# ==============================================================================

SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="mdkabirulhossainj@gmail.com"
SMTP_PASS="your-app-password"
SMTP_SECURE="false"
SMTP_FROM='"Luxe E-Commerce" <no-reply@luxe.com>'
